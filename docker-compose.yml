services:
  backend:
    build: ./backend
    container_name: orbitos-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    env_file:
      - ./backend/.env
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: orbitos-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules   # anonymous volume — keeps container node_modules
      - /app/.next          # anonymous volume — keeps Next.js build cache
    env_file:
      - ./frontend/.env.local
    depends_on:
      - backend
    restart: unless-stopped
